
  <%= tag(:meta, :name => 'description', :content => "") %>
  <%= tag(:meta, :name => 'keywords', :content => "") %>
  <%= tag(:link, :rel => 'shortcut icon', :href => '/favicon.ico') %>

  <%= tag(:meta, :name => 'application-name', :content => "") %>
  <%= tag(:meta, :name => 'msapplication-tooltip', :content => "") %>
